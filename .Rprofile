setwd("_working")
library(showtext)
library(ggplot2)
library(scales)

font.add.google("Poppins", "myfont")
showtext.auto()

theme_set(theme_light(base_family = "myfont") + theme(legend.position = "bottom"))
update_geom_defaults("text", list(family = "myfont"))
